#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, p;
    cin >> n >> p;
    for (int i = 0; i < n; i++)
    {
        string status;
        cin >> status;
    }
    cout << p << endl;
    return 0;
}
