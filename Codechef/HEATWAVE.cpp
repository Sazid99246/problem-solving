#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int x, y;
    cin >> x >> y;
    string result = y > x ? "YES" : "NO";
    cout << result << endl;
    return 0;
}
